const knex = require("../../config/knex.config");
const TOKONES_TABLE = "tokens";
const { DataBaseError, errors } = require("../utils/error.util");

module.exports = class TokensRepository {
    async findToken() {

    }

    async updateToken() {
        
    }

}